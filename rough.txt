# Ensure that all DataFrames have the same columns
    dfs1 = {sheet_name: df.reindex(columns=dfs2[sheet_name].columns, fill_value='NA') for sheet_name, df in dfs1.items()}
    dfs2 = {sheet_name: df.reindex(columns=dfs1[sheet_name].columns, fill_value='NA') for sheet_name, df in dfs2.items()}
