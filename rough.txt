# Iterate through sheets with the same name
    for sheet_name in set(df1_dict.keys()).intersection(df2_dict.keys()):
        # Extract DataFrames for the current sheet
        df1 = df1_dict[sheet_name]
        df2 = df2_dict[sheet_name]
